#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@umijs/block-sdk/node_modules/@babel/traverse/node_modules/jsesc/bin/jsesc" "$@"
  ret=$?
else 
  node  "$basedir/../@umijs/block-sdk/node_modules/@babel/traverse/node_modules/jsesc/bin/jsesc" "$@"
  ret=$?
fi
exit $ret
